# 🚀 วิธีอัปโหลดขึ้น Cloudflare Pages (pages.dev)

ไม่ต้องติดตั้งโปรแกรมอะไรเพิ่ม ไม่ต้องใช้ Git ก็ได้ (ใช้วิธีลากโฟลเดอร์แบบเดียวกับ Netlify Drop)

## ขั้นตอน

1. สมัคร/ล็อกอินบัญชี Cloudflare ฟรีที่ **https://dash.cloudflare.com**
2. เมนูด้านซ้าย เลือก **Workers & Pages**
3. กดปุ่ม **Create** (มุมขวาบน)
4. เลือกแท็บ **Pages** → เลือก **Upload assets**
5. ตั้งชื่อโปรเจกต์ (เช่น `anniversary-site`) → กด **Create project**
6. **ลากทั้งโฟลเดอร์ `anniversary-site`** (ทุกไฟล์ข้างในรวมถึง index.html, css/, js/, images/, music/, _headers) ไปวางในกล่องอัปโหลด
   - ไม่ต้องตั้งค่า Build command หรือ Output directory ใดๆ ทั้งสิ้น เพราะเป็นไฟล์ static ล้วน เว้นว่างไว้ได้เลย
7. กด **Deploy site**
8. รอสักครู่ จะได้ลิงก์เว็บทันที รูปแบบ **`https://ชื่อโปรเจกต์.pages.dev`**

## อัปเดตเว็บทีหลัง (เพิ่มรูป/เพลง/แก้ข้อความ)
1. แก้ไขไฟล์ในโฟลเดอร์เดิมในเครื่องคุณให้เสร็จก่อน
2. กลับไปที่โปรเจกต์นั้นใน Cloudflare dashboard
3. ไปที่แท็บ **Deployments** → กด **Create deployment**
4. ลากโฟลเดอร์ที่แก้ไขแล้วไปวางซ้ำ
5. ลิงก์เดิมจะอัปเดตเนื้อหาใหม่ให้อัตโนมัติ (ลิงก์ไม่เปลี่ยน เพราะผูกกับบัญชีคุณแล้วตั้งแต่แรก)

## ข้อดีเทียบกับ Netlify Drop (แบบไม่ล็อกอิน)
- ลิงก์ **ไม่หมดอายุ ไม่ถูกลบอัตโนมัติ** เพราะผูกกับบัญชีตั้งแต่ขั้นตอนแรก
- ตั้งชื่อโปรเจกต์เองได้ (ได้ลิงก์ที่จำง่ายกว่า)

## ⚠️ อย่าลืมก่อนอัปโหลด
- `music/song.mp3` ใส่ไฟล์เพลงแล้ว
- `js/content-data.js` เขียนข้อความจดหมาย + ปิดท้ายแล้ว
- `js/album-data.js` วางโค้ดจากหน้า admin.html (ถ้าเพิ่มรูปผ่านฟอร์ม) แล้ว
